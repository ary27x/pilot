print("Welcome to the program!!");
firstNumber = 27;
secondNumber = 141;
print("This is the values before the swap : ");
print(firstNumber);
print(secondNumber);
temp = firstNumber; ~ we are swapping the two values using a temp variable
firstNumber = secondNumber;
secondNumber = temp; ~ we assign the second number variable to be the value in temp varialbe
print("This is the values after the swap :");
print(firstNumber);
print(secondNumber);
return 0;
